# Nerdy Pizzas

A small Node.js + Express demo app that renders “nerdy” pizzas from a headless CMS (Directus).  
The app uses server-side rendering with Liquid templates, and supports filtering and sorting via URL parameters.
